import { ServiceProviderModel } from './service-provider-model';

describe('ServiceProviderModel', () => {
  it('should create an instance', () => {
    expect(new ServiceProviderModel()).toBeTruthy();
  });
});
